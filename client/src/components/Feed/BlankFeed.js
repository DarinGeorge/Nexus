import React from 'react'
import { Typography } from '@material-ui/core'

export default function BlankFeed() {
    return (
        <>
            <Typography>There are no posts in your feed. Make connections to get posts from other creatives in your network.</Typography>
        </>
    )
}